### homework
week 5
day 2

### prompt
Print out every character in "{{ str }}", each on a separate line. Hint: Use a
loop. Look up the charAt() method.

### solution
var sentence = "{{ str }}";
var i = 0;
while (i < sentence.length) {
  console.log(sentence.charAt(i));
  i += 1;
}

### test
// pass

### hint
Here's how you can use a while loop to iterate over a string:
<pre>
var sentence = "foobar";
var i = 0;
while (i < sentence.length) {
  console.log(sentence[i]);
  i = i + 1;
}
</pre>

### gen_template_vars
res = {
  'str': rand_sentence()
}
