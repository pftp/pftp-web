### prompt
Write a function called concat_three that takes three strings as arguments and
returns them concatenated together.

### solution
function concat_three(x, y, z) {
  return x + y + z;
}

### test
for (var i in {{ x }}) {
  for (var j in {{ y }}) {
    for (var k in {{ z }}) {
      console.log(concat_three(i, j, k));
    }
  }
}

### hint
Here's code for the concat_two function which takes two strings as
arguments and returns them concatenated together:
<pre>
function concat_two(x, y) {
  return x + y;
}
</pre>

### gen_template_vars
res = {
  'x': rand_word_list(10),
  'y': rand_word_list(10),
  'z': rand_word_list(10)
}

