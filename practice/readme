Files that allow template syntax
prompt
solution.py
test.py


Files that don't allow template syntax
hint


template_variables.json
Template variables specific to a problem  are found in template_variables.json
template_variables.json is in the form of {"variable_name1": ["value1, "value2", "value3"], "variable_name2": ["value4", "value5", "value6"]}

Template Syntax
{{ variable_name }} must be in template_variables.json
{{ random_int:variable_name }} will replace all instances of variable_name inside your code with the same random integer
{{ random_int_list:variable_name }}
{{ random_string_list:variable_name }}
{{ random_word:variable_name }}
{{ random_sentence:variable_name }}
{{ random_s_s_dictionary:variable_name }} will replace all instances of variable name inside your code with the same dictionary of strings to strings
{{ random_s_i_dictionary:variable_name }} will replace all instances of variable name inside your code with the same dictionary of strings to integers
