### prompt
Print out every value in {{ lst }} using a while loop.

### solution
values = {{ lst }}
i = 0
while i < len(values):
  print values[i]
  i += 1

### test
# pass

### hint
Here's how you can use a while loop to iterate over a list:
<pre>
foo = [1,2,3,4,5,6]
i = 0
while i < len(foo):
  print foo[i]
  i = i + 1
</pre>

### gen_template_vars
res = {
  'lst': rand_word_list(rand_int(5, 10)) 
}
