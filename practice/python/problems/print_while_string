### prompt
Print out every character in "{{ str }}" using a while loop.

### solution
sentence = "{{ str }}"
i = 0
while i < len(sentence):
  print sentence[i]
  i += 1

### test
# pass

### hint
Here's how you can use a while loop to iterate over a string:
<pre>
sentence = "foobar"
i = 0
while i < len(sentence):
  print sentence[i]
  i = i + 1
</pre>

### gen_template_vars
res = {
  'str': rand_sentence()
}
