### prompt
Print out every number from {{ start }} to {{ end }} using a for loop.

### solution
for i in range({{ start }}, {{ end }}+1):
  print i

### test
# pass

### hint
Here's how you can write a for loop from 1 to 10:
<pre>
for i in range(1, 10):
  print i
</pre>

### gen_template_vars
res = {
  'start': randint(1, 100),
  'end': randint(200, 300)
}
