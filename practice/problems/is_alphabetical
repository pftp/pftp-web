### prompt
Write a function called is_alphabetical(x, y) that takes two strings x and y as arguments and returns True if and only if x comes before y in the alphabet. Ignore capitalization.

### solution
def is_alphabetical(x, y):
  return x.lower() < y.lower()

### test
for x in {{ x }}:
  for y in {{ x }}:
    print is_alphabetical(x, y)

### hint
Here's code for the is_reverse_alphabetical function which takes two strings x
and y as arguments and returns True if and only if x comes after y in the
alphabet, ignoring capitalization.
<pre>
def is_reverse_alphabetical(x, y):
  return x.lower() > y.lower()
</pre>

### gen_template_vars
res = {
  'x' : rand_word_list(20),
  'y' : rand_word_list(20)
}

